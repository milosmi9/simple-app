pipeline {
    agent any
    stages {
        stage("run frontend"){
            steps {
                echo "exec npm"
                    sh 'pm2 restart 0'
            }    
        }    
    }   
}